name: Metrics
on:
  schedule: [{cron: "0 0 * * *"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: stvshy
          template: classic
          config_timezone: Europe/Warsaw
          
          # --- SZERSZY TRYB WIDOKU ---
          config_display: regular 

          # --- PODSTAWY (BEZ HEADERÓW/KAFELKÓW) ---
          base: activity, repositories
          base_indepth: yes
          
          # --- LINIJKI KODU (ALL-TIME) ---
          plugin_lines: yes
          plugin_lines_history_limit: 0
          plugin_lines_repositories_limit: 100
          plugin_lines_sections: base
          
          # --- JĘZYKI (MAX OKRES + MAX ILOŚĆ) ---
          plugin_languages: yes
          plugin_languages_indepth: yes
          plugin_languages_limit: 16
          plugin_languages_details: lines, percentage
          plugin_languages_sections: most-used
          # Zwiększamy limity czasowe, żeby szukało jak najgłębiej (All-Time):
          plugin_languages_analysis_timeout: 60
          plugin_languages_analysis_timeout_repositories: 15
          
          # Podmiana koloru 1. języka (indeks 0) na błękitny
          plugin_languages_colors: "0:#87CEFA"
          
          # Customowe nazwy języków
          plugin_languages_aliases: "Jupyter Notebook:Jupyter, TSX:TypeScript"
          
          # --- MAGIA STYLOWANIA (TWOJE KOLORY) ---
          extras_css: |
            h2, .header-title { color: #411b89 !important; } 
            .field svg { fill: #319ef7 !important; } 
            
          # --- MAGIA USUWANIA (WYCINAMY ŚMIECI) ---
          extras_js: |
            document.querySelectorAll('.field').forEach(field => {
              const text = field.innerText || "";
              if (
                text.includes('reviewed') || 
                text.includes('Issues') || 
                text.includes('issue') || 
                text.includes('license') || 
                text.includes('Releases') || 
                text.includes('Packages') || 
                text.includes('Forkers') ||
                text.includes('Sponsors')
              ) {
                field.remove();
              }
            });
